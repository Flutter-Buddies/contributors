import 'package:stacked/stacked.dart';

/// [CustomBaseViewModel] is where shared variables and method that are used by
/// the `ViewModels` live.
abstract class CustomBaseViewModel extends BaseViewModel {}
